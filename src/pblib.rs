extern crate libloading;
pub struct PBLib {
    
}

impl PBLib {
    fn new(name: &str) -> PBLib {
        
    }
    fn get_descriptor(self, namespace: &str, message: &str) -> Descriptor {
        unimplemented!()
    }
}


struct Descriptor {}
impl Descriptor {
    fn get_field(self, name: &str) { unimplemented!() }
}
